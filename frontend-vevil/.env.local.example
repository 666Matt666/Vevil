# Variables de entorno para desarrollo local
# Copiar este archivo a .env.local y configurar los valores

# URL del backend (usar /api para desarrollo local con proxy Vite)
VITE_API_URL=/api

# Opcional: URL completa del backend (descomentar si no se usa proxy)
# VITE_API_URL=http://localhost:3000/api
