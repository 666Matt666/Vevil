# Copiá a .env.local para desarrollo en la PC.
# El frontend usará esta URL para hablar con el backend local.
# En la nube (Vercel) se usa la variable VITE_API_URL del dashboard (apuntando a Render).

VITE_API_URL=http://localhost:3000/api
